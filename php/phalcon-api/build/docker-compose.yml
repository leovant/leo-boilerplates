version: "2"

services:
  
  app:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    volumes:
      - ./../:/app
    networks:
      - default-network

  web:
    image: nginx:latest
    ports:
      - "80:80"
      - "443:443"
    restart: unless-stopped
    volumes:
      - ./../:/app
      - ./site.nginx.conf:/etc/nginx/conf.d/default.conf
    links: 
      - "app:api.local"
    networks:
      - default-network

networks:
  default-network:
    driver: bridge